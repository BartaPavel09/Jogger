spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/jogger_db?allowPublicKeyRetrieval=true&useSSL=false"
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
